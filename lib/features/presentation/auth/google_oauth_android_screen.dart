import 'package:flutter/material.dart';

class GoogleOauthAndroidScreen extends StatelessWidget {
  const GoogleOauthAndroidScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Google OAuth Android screen is deprecated.'),
      ),
    );
  }
}
